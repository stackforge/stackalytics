server {
    listen   80;
    location / {
            uwsgi_pass  127.0.0.1:9090;
            include     uwsgi_params;
    }
    location  /static/ {
            alias  /opt/stack/stackalytics/dashboard/static/;
    }
}
